/* 1 */
SELECT COUNT(*) AS Quantidade FROM MEDICAMENTOS

/* 2 */
SELECT AVG(EXTRACT(YEAR FROM CURRENT_DATE) - EXTRACT(YEAR FROM T.DATA_NASCIMENTO)) AS Media_idade FROM TUTOR T

/* 3 */
SELECT MIN(m.PRECO) FROM MEDICAMENTOS m 

/* 4 */
SELECT MAX(P.PESO) FROM PACIENTE P

/* 5 */
SELECT COUNT(*) FROM TUTOR t

/* 6 */
SELECT SUM(M.ESTOQUE) AS VALOR_EM_ESTOQUE
,M.ID_MEDICAMENTO
FROM MEDICAMENTOS M
GROUP BY M.ID_MEDICAMENTO